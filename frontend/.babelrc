{
  "presets": [
    "@babel/preset-env",
    [
      "@babel/preset-react",
      {
        "runtime": "automatic" // <--- CRITICAL FIX: Forces the new transform
      }
    ],
    "@babel/preset-typescript"
  ]
}